services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "8080:8080"
        depends_on:
            - redis
        env_file:
            - .env

    redis:
        image: "redis:latest"
        ports:
            - "6379:6379"
        volumes:
            - ./redis-data:/data
